# Engine ThirdParty

Bundled third-party dependencies used by engine runtime builds live here.

Current contents:

- `glm`
- `imgui`
- `lua`
- `sol`

Prefer package-manager targets such as vcpkg for platform libraries. Keep only vendored headers/sources here when the engine needs them in-tree.
