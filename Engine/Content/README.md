# Engine Content

Reserved for built-in engine resources such as fallback textures, default fonts, debug materials and future editor icons.

Project/game assets belong in the root `Content` folder or in a sample-local `Content` folder.
